const app = Vue.createApp({
    data(){
        return {
            courseGoal :'Finish the course ',
            vueLink : 'https://www.samanja.dev/'
        };
    }
});

app.mount('#user-goal')